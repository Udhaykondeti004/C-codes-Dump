#include <stdio.h>
#include <string.h>

int main(void) {
	char a[20],b[20];
	printf("Enter the string \n");
	scanf("%s",a);
	strcpy(b,a);
	strrev(s2);
	if(strcmp(a,b)==0)
	printf("%s is a palindrome\n",a);
	else
	printf("%s is not a palindrome\n",a);
	return 0;
}

